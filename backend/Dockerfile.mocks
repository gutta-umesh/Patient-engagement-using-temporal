FROM python:3.12-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY mocks/ mocks/

# Determine which mock to run via MOCK_SERVICE env var
CMD ["sh", "-c", "uvicorn mocks.${MOCK_SERVICE}:app --host 0.0.0.0 --port ${MOCK_PORT:-8090}"]
